const sidePotStyles = document.createElement('style');
const pokerStyles = document.createElement('style');
const initialModal = document.getElementById('initial-modal');
const mainGame = document.getElementById('main-game');
const waitingScreen = document.getElementById('waiting-screen');
const playersNeeded = document.getElementById('players-needed');
const playerChipsElement = document.getElementById('player-chips');
const playerBetElement = document.getElementById('player-bet');
const roundEndBtn = document.getElementById('round-end-btn');
const potTotalElement = document.getElementById('pot-total');
const currentSmallBlindElement = document.getElementById('current-small-blind');
const currentBigBlindElement = document.getElementById('current-big-blind');
const playerList = document.getElementById('player-list');
const chatMessages = document.getElementById('chat-messages');
const chatInput = document.getElementById('chat-input');
const sendMessageButton = document.getElementById('send-message');
const charCount = document.getElementById('char-count');
const messageCountDisplay = document.getElementById('message-count');
const startGameBtn = document.getElementById('start-game-btn');
const chipsContainer = document.getElementById('chips-container');
const allInBtn = document.getElementById('all-in-btn');
const foldBtn = document.getElementById('fold-btn');
const checkBtn = document.getElementById('check-btn');
const callBtn = document.getElementById('call-btn');
const currentBetElement = document.getElementById('current-bet');
const currentBetAmount = document.getElementById('current-bet-amount');
const winnerModal = document.getElementById('winner-modal');
const potToWin = document.getElementById('pot-to-win');
const winnerOptions = document.getElementById('winner-options');
const confirmWinnerBtn = document.getElementById('confirm-winner');
const roomCodeDisplay = document.getElementById('room-code-display');
const roomCodeElement = document.getElementById('room-code');
const joinRoomBtn = document.getElementById('join-room-btn');
const roomCodeInput = document.getElementById('room-code-input');
const playerNameInput = document.getElementById('player-name');
const joinPlayerNameInput = document.getElementById('join-player-name');
const tempBetDisplay = document.getElementById('temp-bet-display');
const tempBetValue = document.getElementById('temp-bet-value');
const confirmBetBtn = document.getElementById('confirm-bet-btn');
const cancelBetBtn = document.getElementById('cancel-bet-btn');
const connectionStatus = document.getElementById('connection-status');
const roomInfo = document.getElementById('room-info');
const playerCount = document.getElementById('player-count');
const roomCodeInfo = document.getElementById('room-code-info');
const createRoomBtn = document.getElementById('create-room-btn');
const leaveRoomBtn = document.getElementById('leave-room-btn');
const cancelWinnerBtn = document.getElementById('cancel-winner');
const nextPlayerName = document.getElementById('next-player-name');
const roundModal = document.getElementById('round-modal');
const roundTitle = document.getElementById('round-title');
const roundPhase = document.getElementById('round-phase');
const roundDescription = document.getElementById('round-description');
const closeRoundModal = document.getElementById('close-round-modal');
const style = document.createElement('style');